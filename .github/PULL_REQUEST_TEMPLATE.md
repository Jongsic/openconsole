# Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] User-facing strings added to both `en.json` and `ko.json` (if any)
- [ ] No server-side runtime introduced (app stays browser-only)
